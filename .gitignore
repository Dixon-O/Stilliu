# === SECRETS — NEVER COMMIT ===
.env
*.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.venv/
venv/
env/
ENV/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml

# Live measurement runs — real model output, written by calibrate.py and
# validate_pipeline.py via runlog.py. Kept local: generated prose is not
# something to commit by reflex. Quote the numbers in the README by hand.
backend/results/

# Node / Frontend
node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/
# TypeScript's incremental build cache — regenerated by every tsc run
*.tsbuildinfo

# Submission prep, not the project. The demo voiceover script is a recording
# artefact — timestamps and narration for a video — with nothing in the codebase
# depending on it. Kept locally, out of the repo a judge reads.
DEMO_SCRIPT.md

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
